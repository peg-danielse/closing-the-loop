# --- START: seq=0 ---

apiVersion: v1
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving
data:
  max-scale-up-rate: "1000.0" # Default: 1000.0
  max-scale-down-rate: "2.0" # Default: 2.0
  scale-to-zero-pod-retention-period: "0s"
  scale-to-zero-grace-period: "30s"
  enable-scale-to-zero: "true" # true, false

---

{'max_throughput': 0, 'total_errors': 0, 'total_anomaly_count': 0, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': 0.0, 'total_max_cpu_usage': 0.0}

# --- END: seq=0 ---

# --- START: seq=1 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 195.1, 'total_errors': 3889, 'total_anomaly_count': 19, 'changed_service_anomaly_count': 6, 'service_max_cpu_usage': 0.0813508488676311, 'total_max_cpu_usage': 5.572135062849386, 'service_names': ['srv-profile', 'config-autoscaler']}
# --- END: seq=1 ---

# --- START: seq=2 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 195.1, 'total_errors': 3889, 'total_anomaly_count': 19, 'changed_service_anomaly_count': 6, 'service_max_cpu_usage': 0.0813508488676311, 'total_max_cpu_usage': 5.572135062849386, 'service_names': ['srv-profile', 'config-autoscaler']}
# --- END: seq=2 ---

# --- START: seq=3 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 148.9, 'total_errors': 3288, 'total_anomaly_count': 21, 'changed_service_anomaly_count': 8, 'service_max_cpu_usage': 3.432519676457461, 'total_max_cpu_usage': 4.6454254843488965, 'service_names': ['srv-rate', 'config-autoscaler']}
# --- END: seq=3 ---

# --- START: seq=4 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 148.1, 'total_errors': 3874, 'total_anomaly_count': 15, 'changed_service_anomaly_count': 3, 'service_max_cpu_usage': 1.1252736594020634, 'total_max_cpu_usage': 5.47550164881901, 'service_names': ['srv-search', 'config-autoscaler']}
# --- END: seq=4 ---

# --- START: seq=5 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 143.8, 'total_errors': 4331, 'total_anomaly_count': 22, 'changed_service_anomaly_count': 5, 'service_max_cpu_usage': 1.1440114949060791, 'total_max_cpu_usage': 5.810144913582816, 'service_names': ['srv-search', 'config-autoscaler']}
# --- END: seq=5 ---

# --- START: seq=6 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 164.9, 'total_errors': 4541, 'total_anomaly_count': 14, 'changed_service_anomaly_count': 7, 'service_max_cpu_usage': 0.2715435874917695, 'total_max_cpu_usage': 5.680967092454973, 'service_names': ['srv-reservation', 'config-autoscaler']}
# --- END: seq=6 ---

