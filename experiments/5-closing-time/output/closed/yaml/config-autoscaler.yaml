# --- START: seq=0 ---

apiVersion: v1
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving
data:
  max-scale-up-rate: "1000.0" # Default: 1000.0
  max-scale-down-rate: "2.0" # Default: 2.0
  scale-to-zero-pod-retention-period: "0s"
  scale-to-zero-grace-period: "30s"
  enable-scale-to-zero: "true" # true, false

---

{'max_throughput': 0, 'total_errors': 0, 'total_anomaly_count': 0, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': 0.0, 'total_max_cpu_usage': 0.0}

# --- END: seq=0 ---# --- START: seq=1 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'false'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 9.9, 'total_errors': 221, 'total_anomaly_count': 4, 'changed_service_anomaly_count': 2, 'service_max_cpu_usage': 0.1677937934378535, 'total_max_cpu_usage': 1.0415940445094178, 'service_names': ['srv-search', 'config-autoscaler']}
# --- END: seq=1 ---

# --- START: seq=2 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'false'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 48.3, 'total_errors': 158, 'total_anomaly_count': 2, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': 0.0325410467765715, 'total_max_cpu_usage': 0.3872594072438078, 'service_names': ['srv-reservation', 'config-autoscaler']}
# --- END: seq=2 ---

# --- START: seq=3 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 124.2, 'total_errors': 4082, 'total_anomaly_count': 10, 'changed_service_anomaly_count': 4, 'service_max_cpu_usage': 0.0004166458232805, 'total_max_cpu_usage': 0.4852799459858669, 'service_names': ['srv-rate', 'config-autoscaler']}
# --- END: seq=3 ---

# --- START: seq=4 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'false'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 86.1, 'total_errors': 801, 'total_anomaly_count': 7, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': 0.0035096915096915, 'total_max_cpu_usage': 0.317387816375145, 'service_names': ['srv-user', 'config-autoscaler']}
# --- END: seq=4 ---

# --- START: seq=5 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'false'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '1000.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 30s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 17.5, 'total_errors': 81, 'total_anomaly_count': 6, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': 0.0009864858088456, 'total_max_cpu_usage': 0.0782522792363986, 'service_names': ['srv-reservation', 'config-autoscaler']}
# --- END: seq=5 ---

