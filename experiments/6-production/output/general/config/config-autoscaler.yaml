# --- START: seq=0 ---

apiVersion: v1
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving
data:
  max-scale-up-rate: "1000.0" # Default: 1000.0
  max-scale-down-rate: "2.0" # Default: 2.0
  scale-to-zero-pod-retention-period: "0s"
  scale-to-zero-grace-period: "30s"
  enable-scale-to-zero: "true" # true, false

---

{'max_throughput': 0, 'total_errors': 0, 'total_anomaly_count': 0, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': 0.0, 'total_max_cpu_usage': 0.0}

# --- END: seq=0 ---# --- START: seq=1 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '500.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 384.4, 'total_errors': 2, 'total_anomaly_count': 3, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': 1.0012316703530766, 'total_max_cpu_usage': 1.9384581518236568, 'service_names': ['srv-rate', 'config-autoscaler']}
# --- END: seq=1 ---

# --- START: seq=2 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '500.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 510.4, 'total_errors': 0, 'total_anomaly_count': 5, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': -1, 'total_max_cpu_usage': 2.005519872333351}
# --- END: seq=2 ---

# --- START: seq=3 ---
apiVersion: v1
data:
  enable-scale-to-zero: 'true'
  max-scale-down-rate: '2.0'
  max-scale-up-rate: '500.0'
  scale-to-zero-grace-period: 30s
  scale-to-zero-pod-retention-period: 0s
kind: ConfigMap
metadata:
  name: config-autoscaler
  namespace: knative-serving

---
{'max_throughput': 503.2, 'total_errors': 2, 'total_anomaly_count': 2, 'changed_service_anomaly_count': 0, 'service_max_cpu_usage': -1, 'total_max_cpu_usage': 2.0089777803535935}
# --- END: seq=3 ---

